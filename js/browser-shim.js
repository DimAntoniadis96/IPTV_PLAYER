/* =====================================================================
 * browser-shim.js — DEPRECATED / UNUSED
 * ---------------------------------------------------------------------
 * This early prototype shim has been superseded by the modular app. TV vs
 * browser is now handled by feature detection inside each subsystem
 * (AVPlayer falls back to HTML5 <video>; RemoteControl maps desktop keys),
 * so no separate shim is loaded. This file is intentionally left empty and
 * is not imported anywhere. It can be safely deleted.
 * ===================================================================== */
export {};
